# VM control library (winvm.py)
paramiko>=3.4
Pillow>=10.0

# Lightweight agent (agent.py) — provider SDKs via backends.py
anthropic>=0.40
openai>=1.40

# DSPy agent (agent_dspy.py) — multi-provider via LiteLLM
dspy>=2.5
optuna>=3.0          # required by optimize.py's MIPROv2 search
